# How will you set the starting value in generating random numbers?

import random
random.seed(10)   #but it will give the same random number because computers work like that 
print(random.randint(1, 100))

